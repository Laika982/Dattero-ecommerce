```hbs
{{>user/header}}

<!-- Main Content Canvas -->
<main class="w-full max-w-max-width mx-auto px-container-padding py-stack-lg md:py-section-gap">

    <div class="max-w-max-width mx-auto px-container-padding mt-6">

    {{> user/notification}}

    </div>


    <!-- =========================
         Breadcrumb
    ========================== -->

    <nav aria-label="Breadcrumb" class="flex text-sm text-outline mb-stack-md">

        <ol class="inline-flex items-center space-x-1 md:space-x-3">

            <li class="inline-flex items-center">

                <a
                    class="font-label-md text-label-md hover:text-ink-black transition-colors"
                    href="/"
                >
                    Home
                </a>

            </li>

            <li>

                <div class="flex items-center">

                    <span class="material-symbols-outlined text-[16px] mx-1">
                        chevron_right
                    </span>

                    <a
                        class="font-label-md text-label-md hover:text-ink-black transition-colors"
                        href="/products"
                    >
                        Products
                    </a>

                </div>

            </li>

            {{#if product.category_id}}

            <li>

                <div class="flex items-center">

                    <span class="material-symbols-outlined text-[16px] mx-1">
                        chevron_right
                    </span>

                    <a
                        class="font-label-md text-label-md hover:text-ink-black transition-colors"
                        href="/products?category={{product.category_id._id}}"
                    >
                        {{product.category_id.category_name}}
                    </a>

                </div>

            </li>

            {{/if}}

            <li aria-current="page">

                <div class="flex items-center">

                    <span class="material-symbols-outlined text-[16px] mx-1">
                        chevron_right
                    </span>

                    <span class="font-label-md text-label-md text-ink-black">
                        {{product.productName}}
                    </span>

                </div>

            </li>

        </ol>

    </nav>


    <!-- =========================
         Product Hero Section
    ========================== -->

    <section class="grid grid-cols-1 lg:grid-cols-12 gap-grid-gutter mb-section-gap">


        <!-- =========================
             Gallery Left
        ========================== -->

        <div class="lg:col-span-7 flex flex-col-reverse md:flex-row gap-stack-md h-full">


            <!-- Thumbnails -->

            <div
                class="flex md:flex-col gap-stack-sm overflow-x-auto md:overflow-y-auto md:w-24 shrink-0 no-scrollbar"
            >

                {{#each product.images}}

                <button
                    type="button"
                    onclick="changeProductImage('{{this.url}}')"
                    class="w-20 h-24 md:w-full md:h-24 bg-surface-gray rounded-xl border {{#if @first}}border-2 border-ink-black{{else}}border-transparent hover:border-ink-black/30{{/if}} overflow-hidden shrink-0 opacity-70 hover:opacity-100 transition-colors"
                >

                    <img
                        class="w-full h-full object-cover"
                        src="{{this.url}}"
                        alt="{{../product.productName}}"
                    >

                </button>

                {{/each}}

            </div>


            <!-- Main Image -->

            <div
                id="zoomContainer"
                class="flex-grow bg-surface-gray rounded-3xl overflow-hidden relative min-h-[500px] md:min-h-[700px]"
            >

                <!-- Product Highlights -->

                <div class="absolute top-6 left-6 flex flex-col gap-2 z-10">

                    {{#if product.highlights}}

                        {{#each product.highlights}}

                        <span
                            class="bg-white/90 backdrop-blur-md px-4 py-1.5 rounded-full border border-outline-variant/30 text-ink-black font-label-md text-label-md shadow-sm"
                        >
                            {{this}}
                        </span>

                        {{/each}}

                    {{else}}

                        <span
                            class="bg-white/90 backdrop-blur-md px-4 py-1.5 rounded-full border border-outline-variant/30 text-ink-black font-label-md text-label-md shadow-sm"
                        >
                            Premium Quality
                        </span>

                    {{/if}}

                </div>


                <!-- Wishlist -->

                <button
                    type="button"
                    class="absolute top-6 right-6 bg-white/90 backdrop-blur-md p-3 rounded-full border border-outline-variant/30 hover:bg-white transition-colors z-10 shadow-sm"
                >

                    <span class="material-symbols-outlined text-ink-black">
                        favorite
                    </span>

                </button>


                <!-- Main Product Image -->

                {{#if product.images.length}}

                <img
                    id="mainProductImage"
                    class="w-full h-full object-cover absolute inset-0 transition-transform duration-300"
                    src="{{product.images.[0].url}}"
                    alt="{{product.productName}}"
                >

                {{else}}

                <div class="w-full h-full absolute inset-0 flex items-center justify-center">

                    <span class="text-outline">
                        No image available
                    </span>

                </div>

                {{/if}}

            </div>

        </div>


        <!-- =========================
             Product Info Right
        ========================== -->

        <div class="lg:col-span-5 flex flex-col pt-stack-md lg:pt-0 pl-0 lg:pl-stack-lg justify-center">


            <!-- Product Heading -->

            <div class="mb-stack-lg">

                <h1
                    class="font-display-lg-mobile md:font-display-lg text-display-lg-mobile md:text-display-lg text-ink-black mb-2 tracking-tight"
                >
                    {{product.productName}}
                </h1>


                <!-- Rating -->

                <div class="flex items-center gap-stack-sm mb-stack-md">

                    <div class="flex text-accent-blue">

                        <span
                            class="material-symbols-outlined text-[18px]"
                            data-weight="fill"
                        >
                            star
                        </span>

                        <span
                            class="material-symbols-outlined text-[18px]"
                            data-weight="fill"
                        >
                            star
                        </span>

                        <span
                            class="material-symbols-outlined text-[18px]"
                            data-weight="fill"
                        >
                            star
                        </span>

                        <span
                            class="material-symbols-outlined text-[18px]"
                            data-weight="fill"
                        >
                            star
                        </span>

                        <span
                            class="material-symbols-outlined text-[18px]"
                            data-weight="fill"
                        >
                            star_half
                        </span>

                    </div>

                    <a
                        class="text-on-surface-variant text-sm underline hover:text-ink-black transition-colors"
                        href="#reviews"
                    >
                        {{averageRating}}
                        ({{totalReviews}} Reviews)
                    </a>

                </div>


                <!-- Price -->

                <p
                    class="font-headline-xl text-headline-xl text-ink-black mb-stack-md tracking-tight"
                >

                    ₹{{listingPrice}}

                    {{#if regularPrice}}

                        {{#if discountPercentage}}

                        <span
                            class="text-lg font-normal text-outline line-through ml-2"
                        >
                            ₹{{regularPrice}}
                        </span>

                        {{/if}}

                    {{/if}}

                </p>


                <!-- Discount -->

                {{#if discountPercentage}}

                <p class="text-sm text-green-700 font-medium mb-stack-md">

                    {{discountPercentage}}% OFF

                </p>

                {{/if}}


                <!-- Description -->

                <p
                    class="font-body-lg text-body-lg text-on-surface-variant mb-stack-lg"
                >
                    {{product.productDescription}}
                </p>

            </div>


            <hr class="border-outline-variant/30 mb-stack-lg">


            <!-- =========================
                 Variant Selection
            ========================== -->

            <div class="mb-stack-lg space-y-6">

                <div class="space-y-3">

                    <div class="flex items-center justify-between">

                        <label class="font-label-md text-label-md text-ink-black">
                            Select Size
                        </label>

                        <span class="text-xs text-on-surface-variant font-medium">
                            Choose your variant
                        </span>

                    </div>


                    <div class="grid grid-cols-3 gap-3">

                        {{#each variants}}

                        <button
                            type="button"
                            class="variant-button relative flex flex-col items-center justify-center p-3 rounded-2xl border border-outline-variant/40 bg-surface hover:border-ink-black/40 transition-all text-center group"
                            data-variant-id="{{this._id}}"
                            data-price="{{this.effectivePrice}}"
                            data-stock="{{this.stock_quantity}}"
                            onclick="selectVariant(this)"
                        >

                            <span class="font-label-md text-xs text-ink-black">
                                {{this.weight}}
                            </span>

                            <span class="text-[11px] text-on-surface-variant mt-0.5">
                                {{this.sku}}
                            </span>

                            <span class="text-xs font-semibold text-ink-black mt-1.5">
                                ₹{{this.effectivePrice}}
                            </span>

                        </button>

                        {{/each}}

                    </div>

                </div>

            </div>


            <!-- =========================
                 Stock
            ========================== -->

            <div class="mb-stack-lg">

                {{#if isOutOfStock}}

                <div class="flex items-center gap-2 text-red-600">

                    <span class="material-symbols-outlined text-[20px]">
                        error
                    </span>

                    <span class="font-label-md">
                        Out of Stock
                    </span>

                </div>

                {{else}}

                <div class="flex items-center gap-2 text-green-700">

                    <span class="material-symbols-outlined text-[20px]">
                        check_circle
                    </span>

                    <span class="font-label-md">
                        In Stock
                    </span>

                    {{#if totalStock}}

                    <span class="text-sm text-on-surface-variant">
                        ({{totalStock}} available)
                    </span>

                    {{/if}}

                </div>

                {{/if}}

            </div>


            <!-- =========================
                 Quantity
            ========================== -->

            <div class="space-y-stack-md mb-stack-lg">

                <div class="flex items-center justify-between">

                    <label class="font-label-md text-label-md text-ink-black">
                        Quantity
                    </label>

                    <div
                        class="flex items-center border border-outline-variant/50 rounded-lg bg-surface h-12 w-32"
                    >

                        <button
                            type="button"
                            onclick="decreaseQuantity()"
                            class="px-4 text-ink-black hover:bg-surface-gray transition-colors h-full rounded-l-lg"
                        >
                            -
                        </button>

                        <input
                            id="quantity"
                            class="flex-grow w-full text-center bg-transparent border-none text-ink-black font-label-md focus:ring-0 p-0 m-0 h-full"
                            type="number"
                            value="1"
                            min="1"
                        >

                        <button
                            type="button"
                            onclick="increaseQuantity()"
                            class="px-4 text-ink-black hover:bg-surface-gray transition-colors h-full rounded-r-lg"
                        >
                            +
                        </button>

                    </div>

                </div>

            </div>


            <!-- =========================
                 Buttons
            ========================== -->

            <div class="flex flex-col gap-stack-md mb-stack-lg">

                {{#if isOutOfStock}}

                <button
                    type="button"
                    disabled
                    class="w-full bg-surface-gray text-outline font-label-md text-label-md py-4 rounded-2xl cursor-not-allowed"
                >
                    Out of Stock
                </button>

                {{else}}

                <button
                    type="button"
                    id="addToCartButton"
                    class="w-full bg-ink-black text-white font-label-md text-label-md py-4 rounded-2xl hover:bg-ink-black/90 transition-colors flex items-center justify-center gap-2 shadow-sm"
                >

                    <span class="material-symbols-outlined text-[20px]">
                        shopping_bag
                    </span>

                    Add to Cart

                </button>


                <button
                    type="button"
                    id="buyNowButton"
                    class="w-full bg-surface border border-outline-variant/50 text-ink-black font-label-md text-label-md py-4 rounded-2xl hover:bg-surface-gray transition-colors"
                >
                    Buy It Now
                </button>

                {{/if}}

            </div>


            <!-- =========================
                 Trust Badges
            ========================== -->

            <div
                class="grid grid-cols-3 gap-unit pt-stack-md border-t border-outline-variant/30 mt-stack-lg"
            >

                <div class="flex flex-col items-center text-center gap-2">

                    <span class="material-symbols-outlined text-ink-black text-2xl">
                        verified
                    </span>

                    <span class="text-sm text-on-surface-variant font-label-md">
                        Premium Quality
                    </span>

                </div>


                <div class="flex flex-col items-center text-center gap-2">

                    <span class="material-symbols-outlined text-ink-black text-2xl">
                        local_shipping
                    </span>

                    <span class="text-sm text-on-surface-variant font-label-md">
                        Fast Delivery
                    </span>

                </div>


                <div class="flex flex-col items-center text-center gap-2">

                    <span class="material-symbols-outlined text-ink-black text-2xl">
                        eco
                    </span>

                    <span class="text-sm text-on-surface-variant font-label-md">
                        Quality Ingredients
                    </span>

                </div>

            </div>

        </div>

    </section>


    <!-- =========================
         Product Details Tabs
    ========================== -->

    <section class="mb-section-gap">

        <div
            class="border-b border-outline-variant/30 flex space-x-8 mb-stack-lg overflow-x-auto no-scrollbar"
        >

            <button
                class="pb-3 border-b-2 border-ink-black font-label-md text-label-md text-ink-black whitespace-nowrap"
            >
                Product Details
            </button>

            <button
                class="pb-3 border-b-2 border-transparent hover:border-outline-variant font-label-md text-label-md text-on-surface-variant hover:text-ink-black transition-all whitespace-nowrap"
            >
                Nutritional Facts
            </button>

            <button
                class="pb-3 border-b-2 border-transparent hover:border-outline-variant font-label-md text-label-md text-on-surface-variant hover:text-ink-black transition-all whitespace-nowrap"
            >
                Reviews
            </button>

        </div>


        <div class="grid grid-cols-1 md:grid-cols-2 gap-grid-gutter">


            <!-- =========================
                 Product Highlights
            ========================== -->

            <div class="font-body-lg text-body-lg text-on-surface-variant space-y-6 pt-4">

                <p>
                    {{product.productDescription}}
                </p>


                {{#if product.highlights}}

                <ul class="list-none space-y-4">

                    {{#each product.highlights}}

                    <li class="flex items-start gap-3">

                        <span
                            class="material-symbols-outlined text-ink-black text-[20px] mt-0.5"
                        >
                            check_circle
                        </span>

                        <span>
                            {{this}}
                        </span>

                    </li>

                    {{/each}}

                </ul>

                {{/if}}

            </div>


            <!-- =========================
                 Nutrition Table
            ========================== -->

            <div
                class="bg-surface-gray rounded-3xl p-8 border border-outline-variant/20"
            >

                <h3
                    class="font-headline-lg text-xl font-semibold text-ink-black mb-6 border-b border-outline-variant/20 pb-4 tracking-tight"
                >
                    Product Information
                </h3>


                <div class="space-y-4">

                    <div class="flex justify-between border-b border-outline-variant/20 pb-3">

                        <span class="font-body-lg text-on-surface-variant">
                            Brand
                        </span>

                        <span class="font-label-md text-ink-black">
                            {{product.brand}}
                        </span>

                    </div>


                    <div class="flex justify-between border-b border-outline-variant/20 pb-3">

                        <span class="font-body-lg text-on-surface-variant">
                            Category
                        </span>

                        <span class="font-label-md text-ink-black">

                            {{product.category_id.category_name}}

                        </span>

                    </div>


                    <div class="flex justify-between border-b border-outline-variant/20 pb-3">

                        <span class="font-body-lg text-on-surface-variant">
                            Available Variants
                        </span>

                        <span class="font-label-md text-ink-black">
                            {{variants.length}}
                        </span>

                    </div>


                    <div class="flex justify-between pt-2">

                        <span class="font-body-lg text-on-surface-variant">
                            Stock
                        </span>

                        <span class="font-label-md text-ink-black">

                            {{#if isOutOfStock}}
                                Out of Stock
                            {{else}}
                                In Stock
                            {{/if}}

                        </span>

                    </div>

                </div>

            </div>

        </div>

    </section>


    <!-- =========================
         Reviews
    ========================== -->

    <section id="reviews" class="mb-section-gap">

        <div class="mb-stack-lg">

            <h2 class="font-headline-lg text-2xl font-semibold text-ink-black">
                Customer Reviews
            </h2>

            <p class="text-on-surface-variant text-sm mt-2">
                {{averageRating}} out of 5
                ·
                {{totalReviews}} Reviews
            </p>

        </div>


        {{#if reviews.length}}

        <div class="space-y-6">

            {{#each reviews}}

            <div class="border-b border-outline-variant/30 pb-6">

                <div class="flex items-center justify-between mb-2">

                    <div>

                        <p class="font-label-md text-ink-black">
                            {{this.user_id.name}}
                        </p>

                        <div class="flex text-accent-blue mt-1">

                            <span class="material-symbols-outlined text-[16px]">
                                star
                            </span>

                            <span class="material-symbols-outlined text-[16px]">
                                star
                            </span>

                            <span class="material-symbols-outlined text-[16px]">
                                star
                            </span>

                            <span class="material-symbols-outlined text-[16px]">
                                star
                            </span>

                            <span class="material-symbols-outlined text-[16px]">
                                star
                            </span>

                        </div>

                    </div>

                </div>

                <p class="text-on-surface-variant text-sm">
                    {{this.review}}
                </p>

            </div>

            {{/each}}

        </div>

        {{else}}

        <div class="py-10 text-center">

            <p class="text-on-surface-variant">
                No reviews yet.
            </p>

        </div>

        {{/if}}

    </section>


    <!-- =========================
         Related Products
    ========================== -->

    {{#if relatedProducts.length}}

    <section class="mb-section-gap">

        <div class="mb-stack-lg">

            <h2 class="font-headline-lg text-2xl font-semibold text-ink-black">
                You May Also Like
            </h2>

        </div>


        <div class="grid grid-cols-2 md:grid-cols-4 gap-grid-gutter">

            {{#each relatedProducts}}

            <a
                href="/products/{{this._id}}"
                class="group"
            >

                <div class="bg-surface-gray rounded-2xl overflow-hidden aspect-square mb-3">

                    {{#if this.images.length}}

                    <img
                        src="{{this.images.[0].url}}"
                        alt="{{this.productName}}"
                        class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-300"
                    >

                    {{/if}}

                </div>


                <h3 class="font-label-md text-ink-black">
                    {{this.productName}}
                </h3>

            </a>

            {{/each}}

        </div>

    </section>

    {{/if}}

</main>


<!-- =========================
     Product Page JavaScript
========================== -->

<script>

    // -------------------------------
    // Change main product image
    // -------------------------------

    function changeProductImage(url) {

        const mainImage =
            document.getElementById("mainProductImage");

        if (!mainImage) return;

        mainImage.src = url;
    }


    // -------------------------------
    // Quantity
    // -------------------------------

    function increaseQuantity() {

        const quantity =
            document.getElementById("quantity");

        if (!quantity) return;

        quantity.value =
            Number(quantity.value) + 1;
    }


    function decreaseQuantity() {

        const quantity =
            document.getElementById("quantity");

        if (!quantity) return;

        const current =
            Number(quantity.value);

        if (current > 1) {

            quantity.value = current - 1;

        }
    }


    // -------------------------------
    // Variant selection
    // -------------------------------

    let selectedVariantId = null;


    function selectVariant(button) {

        const buttons =
            document.querySelectorAll(
                ".variant-button"
            );


        buttons.forEach(item => {

            item.classList.remove(
                "border-2",
                "border-ink-black"
            );

            item.classList.add(
                "border",
                "border-outline-variant/40"
            );

        });


        button.classList.remove(
            "border",
            "border-outline-variant/40"
        );

        button.classList.add(
            "border-2",
            "border-ink-black"
        );


        selectedVariantId =
            button.dataset.variantId;


        const stock =
            Number(button.dataset.stock);


        const addToCartButton =
            document.getElementById(
                "addToCartButton"
            );


        if (!addToCartButton) return;


        if (stock <= 0) {

            addToCartButton.disabled = true;

            addToCartButton.classList.add(
                "opacity-50",
                "cursor-not-allowed"
            );

        } else {

            addToCartButton.disabled = false;

            addToCartButton.classList.remove(
                "opacity-50",
                "cursor-not-allowed"
            );

        }

    }


    // -------------------------------
    // Image zoom
    // -------------------------------

    const zoomContainer =
        document.getElementById(
            "zoomContainer"
        );

    const mainProductImage =
        document.getElementById(
            "mainProductImage"
        );


    if (
        zoomContainer &&
        mainProductImage
    ) {

        zoomContainer.addEventListener(
            "mousemove",
            function (event) {

                const rect =
                    zoomContainer.getBoundingClientRect();

                const x =
                    ((event.clientX - rect.left) /
                        rect.width) *
                    100;

                const y =
                    ((event.clientY - rect.top) /
                        rect.height) *
                    100;


                mainProductImage.style.transform =
                    "scale(1.5)";

                mainProductImage.style.transformOrigin =
                    `${x}% ${y}%`;

            }
        );


        zoomContainer.addEventListener(
            "mouseleave",
            function () {

                mainProductImage.style.transform =
                    "scale(1)";

                mainProductImage.style.transformOrigin =
                    "center";

            }
        );

    }

</script>


{{>user/footer}}

